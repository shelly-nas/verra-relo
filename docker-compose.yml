services:
  verra-ind-alerts:
    build: .
    container_name: verra-ind-alerts
    ports:
      - "9000:8080"
    volumes:
      # Mount data directory to persist output files
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
